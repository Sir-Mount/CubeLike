/// @desc ScreenShake(FlashAlpha, FlashDecay, FlashColor);
/// @arg FlashAlpha sets the start alpha of the flash.
/// @arg FlashSpeed sets the speed at which the flash will fade out.
/// @arg FlashColor deteremines the color of the flash.
function ScreenFlash(argument0, argument1, argument2) {

	with(Ocam){
		flashAlpha = argument0;
		flashSpeed = argument1;
		flashColor = argument2;
	}


}
